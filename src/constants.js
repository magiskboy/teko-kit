const CDN_STATIC_PATH = 'https://shopfront-cdn.tekoapis.com/static/';

const STATIC_TEMPORARY_DIRECTORY = '/tmp/static';

const KIT_DIRECTORY = 'kit';

const DEFAULT_PACKAGE_OUTPUT = 'dist';

const PACKAGE_BUILD_CONFIG_NAME = 'rollup.config.js';

module.exports = {
  CDN_STATIC_PATH,
  STATIC_TEMPORARY_DIRECTORY,
  KIT_DIRECTORY,
  DEFAULT_PACKAGE_OUTPUT,
  PACKAGE_BUILD_CONFIG_NAME,
};
